import { businessProfiles } from "../data/profile";
import { Reveal } from "./Reveal";
export function BusinessProfile({ brand }: { brand: "movie" | "aisa" }) { const data = businessProfiles[brand]; return <section id="profile" className={`${brand}-section business-profile`}><Reveal><p className="eyebrow">PROFILE</p><h2>{data.heading}</h2></Reveal><div className="profile-layout"><Reveal className="profile-person"><div className="profile-placeholder" aria-label="プロフィール写真は準備中"><span>SS</span></div><p className="profile-name">真田 紗織</p><p>{data.title}</p></Reveal><Reveal className="profile-copy" delay={.08}>{data.paragraphs.map(p=><p key={p}>{p}</p>)}</Reveal></div><div className="profile-strength-grid">{data.strengths.map((item,i)=><Reveal key={item.title} delay={i*.08}><article><h3>{item.title}</h3><p>{item.text}</p></article></Reveal>)}</div></section>; }
